package com.aqa.jhipster.api.enumeration;

import com.aqa.core.enumerations.PropertyReader;

public enum Property implements PropertyReader {
    ADMIN,
    USER,
    ADMIN_PASSWORD,
    USER_PASSWORD;
}